<UserControl x:Class="CinemaManager.Modules.Room.RoomModuleView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CinemaManager.Modules.Room"
      xmlns:filter="clr-namespace:CinemaManager.Filter"
      xmlns:dynamicGrid="clr-namespace:CinemaManager.DynamicGrid"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	d:DataContext="{d:DesignInstance local:RoomModule}">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1*"/>
			<ColumnDefinition Width="5*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition />
			<RowDefinition />
		</Grid.RowDefinitions>
		<filter:FilterConfiguratorView DataContext="{Binding RoomFilterConfigurator}" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="1"/>
		<ListView ItemsSource="{Binding RoomList}" Grid.Row="1" Grid.Column="0" Grid.RowSpan="2"
		          SelectedItem="{Binding SelectetRoom}" SelectionMode="Single">
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="IsSelected" Value="{Binding Path=IsActive, Mode=TwoWay}" />
				</Style>
			</ListView.ItemContainerStyle>
			<ListView.ItemTemplate>
				<DataTemplate>
					<StackPanel>
						<TextBlock Text="{Binding RoomNumber}" />
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
		<Grid Grid.Column="1" Grid.Row="1"  Grid.RowSpan="2" dynamicGrid:GridHelpers.RowCount="6" dynamicGrid:GridHelpers.ColumnCount="3">
			
		</Grid>
	</Grid>
</UserControl>
