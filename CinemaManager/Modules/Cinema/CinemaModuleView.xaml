<UserControl x:Class="CinemaManager.Modules.Cinema.CinemaModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:filter="clr-namespace:CinemaManager.Filter"
             xmlns:cinema="clr-namespace:CinemaManager.Modules.Cinema"
             xmlns:model="clr-namespace:CinemaManager.Model"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance cinema:CinemaModule}">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="8*" />
		</Grid.RowDefinitions>
		<filter:FilterConfiguratorView DataContext="{Binding CinemaFilterConfigurator}" Grid.ColumnSpan="2" />
		<ListView ItemsSource="{Binding Cinemas}" Grid.Row="1" Grid.Column="0"
		          SelectedItem="{Binding CurrentItem}"
		          SelectionChanged="ListView_SelectionChanged">
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="IsSelected" Value="{Binding Path=IsActive, Mode=TwoWay}" />
				</Style>
			</ListView.ItemContainerStyle>
			<ListView.ItemTemplate>
				<DataTemplate>
					<StackPanel>
						<TextBlock Text="{Binding Name}" />
						<TextBlock Text="{Binding Address}" />
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
		<Grid Grid.Row="1" Grid.Column="1">
			<Grid.RowDefinitions>
				<RowDefinition />
				<RowDefinition />
				<RowDefinition />
				<RowDefinition />
			</Grid.RowDefinitions>
			<TextBox Text="{Binding CurrentItem.Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="0"/>
			<TextBox Text="{Binding CurrentItem.Address, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="1"/>
			<Button Command="{Binding AddCinemaCommand}" Content="+" Grid.Row="2"/>
			<Button Command="{Binding RemoveCinemaCommand}" Content="-" Grid.Row="3"/>
		</Grid>
	</Grid>
</UserControl>